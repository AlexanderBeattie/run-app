## Sniper Protocol (v1.9.0)
- **Atomic Commits**: Every change must be a single, logical unit.
- **Context Pruning**: Use `.claudaignore` for `node_modules`, `dist`, and `*.log`.
- **Model Routing**: Use Sonnet for implementation, Opus for architecture/debugging.
